name: Integrate
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
permissions:
  contents: read
jobs:
  lint:
    runs-on: ubuntu-24.04
    name: Lint
    steps:
      - uses: bavulapati/actions/python-base@add-python-base # TEMP fork ref to confirm CI; flip to holepunchto/actions/python-base@v1 before merge
      - run: ruff format --check .
      - run: ruff check .
  test:
    strategy:
      matrix:
        python-version:
          - "3.10"
          - "3.11"
          - "3.12"
          - "3.13"
    runs-on: ubuntu-24.04
    name: Test / ${{ matrix.python-version }}
    steps:
      - uses: bavulapati/actions/python-base@add-python-base # TEMP fork ref to confirm CI; flip to holepunchto/actions/python-base@v1 before merge
        with:
          python-version: ${{ matrix.python-version }}
      - run: pytest
